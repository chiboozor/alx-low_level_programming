#include "main.h"

/**
 * main - program entry point.
 * Return: 0 on success.
 *
 * positive_or_negative: fxn used to test whether a number is +ve or -ve.
 */

int main(void)
{
	int i;

	i = 0;
	positive_or_negative(i);

	return (0);
}
