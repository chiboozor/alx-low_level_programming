#include <stdio.h>

/**
 * main - program entry point.
 * puts: fxn printing to the screen.
 * Return: non-negative number, in this case always 0.
 */

int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
