#include <stdio.h>

/**
 * main - program entry point.
 * printf: function used to print characters to the stdout.
 * Return: always 0.
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art, \n");
	return (0);
}
